const home = '/';
const previousOrders = `${home}previousOrders`;
const restaurants = `${home}restaurants`;
const login = `${home}login`;
const register = `${home}register`;

export default {
    home,
    previousOrders,
    restaurants,
    register,
    login
}